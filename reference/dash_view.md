# Open the dashboard in a browser

Open the dashboard in a browser

## Usage

``` r
dash_view(repo = here("strava_repo"))
```

## Arguments

- repo:

  Path to the Strava repository.

## Value

Path to the index page, invisibly.
