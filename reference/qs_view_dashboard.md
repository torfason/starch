# Open the static Quarto dashboard in a browser

Open the static Quarto dashboard in a browser

## Usage

``` r
qs_view_dashboard(repo = here("strava_repo"))
```

## Arguments

- repo:

  Path to the Strava repository.

## Value

Path to the index page, invisibly.
