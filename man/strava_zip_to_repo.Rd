% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strava_repo.R
\name{strava_zip_to_repo}
\alias{strava_zip_to_repo}
\title{Import a Strava export archive into a version-controlled repository}
\usage{
strava_zip_to_repo(
  zip = latest_strava_zip(),
  repo = here("strava_repo"),
  commit = TRUE,
  author = NULL,
  quiet = FALSE
)
}
\arguments{
\item{zip}{Path to a Strava export archive. Defaults to the most recent
archive found by \code{\link[=latest_strava_zip]{latest_strava_zip()}}.}

\item{repo}{Path to the repository the export is imported into.}

\item{commit}{Whether to commit the result. When \code{FALSE} the changes are
left in the working tree, which will block the next import until they are
dealt with.}

\item{author}{Optional git signature for the commit, as accepted by
\code{\link[gert:git_commit]{gert::git_commit()}}. When \code{NULL}, the configured git identity is used and
its absence is reported before any files are written.}

\item{quiet}{Suppress progress reporting.}
}
\value{
The \code{gert::git_status()} table of what changed, invisibly.
}
\description{
Extracts a Strava bulk export into a git repository, gzips the track files
that arrived uncompressed, and commits the result. Running it against
successive exports builds a version-controlled history of the archive, in
which each commit is one export and the diff is whatever actually changed.
}
\details{
Every export is extracted in full, overwriting what is already there.
Activities can be edited in Strava after the fact, so the presence of a file
in the repository says nothing about whether its content is current. Git,
not the file system, determines what changed.
}
\section{Repository preconditions}{

Because the import overwrites files without prompting, \code{repo} must be in one
of exactly two states, checked before anything is written:
\itemize{
\item \strong{Empty or absent} - the directory is created, a repository is
initialised, and a \code{.gitignore} is written.
\item \strong{A git repository with a clean working tree} - no staged changes, no
unstaged changes, and no untracked files. Ignored files do not count.
}

Anything else is an error, on the assumption that it is an unrelated
directory. The clean-tree requirement also makes a failed import
recoverable: nothing predates the run, so \verb{git reset --hard} undoes it.

Archives containing entries under \verb{.git/}, absolute paths, or parent
traversal are rejected outright.
}

