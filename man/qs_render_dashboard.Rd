% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto_static_dashboard.R
\name{qs_render_dashboard}
\alias{qs_render_dashboard}
\title{Render the static Quarto dashboard}
\usage{
qs_render_dashboard(
  repo = here("strava_repo"),
  max_files = 10,
  max_points = 600,
  verbose = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{Path to the Strava repository.}

\item{max_files}{Maximum number of activities to render in one call.}

\item{max_points}{Number of stream points kept in each page's charts. The
streams run to tens of thousands of points, which no chart can resolve and
which dominate the size of the rendered page, so they are thinned evenly
on the way in. Statistics are computed from the full stream regardless, so
this affects only chart resolution and file size. Use \code{0} to keep every
point, which is worth doing for a single activity examined closely.}

\item{verbose}{Pass the Quarto CLI's own output through, which is verbose
but is the only way to see why a render failed.}

\item{quiet}{Suppress starch's own progress reporting. Independent of
\code{verbose}: the default reports progress without the CLI's output.}
}
\value{
Path to the index page, invisibly.
}
\description{
Renders outstanding activity pages, then rebuilds the index so that it
links to whatever now exists.
}
