% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_columns.R
\name{derive_columns}
\alias{derive_columns}
\alias{addcols_time}
\alias{addcols_distance}
\alias{addcols_speed}
\alias{addcols_speed_naive}
\title{Add derived columns to a stream tibble}
\usage{
addcols_time(d)

addcols_distance(d)

addcols_speed(d, window = 5)

addcols_speed_naive(d)
}
\arguments{
\item{d}{A stream tibble from \code{\link[=read_stream]{read_stream()}}, ordered by \code{timestamp}.}

\item{window}{Half-width, in points, of the centred moving-average window
used to smooth instantaneous speed (\code{.before}/\code{.after} in
\code{\link[slider:slide_dbl]{slider::slide_dbl()}}).}
}
\value{
\code{d} with the columns described for each function inserted after the
relevant existing column.
}
\description{
A family of transforms that add elapsed time, cumulative distance, and speed
to a stream tibble from \code{\link[=read_stream]{read_stream()}}. They are pure \code{tibble} -> \code{tibble}
functions and assume rows are ordered by \code{timestamp} (and that \code{d} is
non-empty). Distance and speed additionally require \code{lat}/\code{lng}; speed
requires that \code{time} and \code{distance} already exist, so the usual order is
\code{addcols_time()} -> \code{addcols_distance()} -> \code{addcols_speed()}.
}
\details{
\code{addcols_speed()} and \code{addcols_speed_naive()} are alternatives that both emit
\code{speed_kmh} and \code{pace}, so apply only one of them.
}
\section{Functions}{
\itemize{
\item \code{addcols_time()}: Add cumulative elapsed \code{time} (seconds from the
first point).

\item \code{addcols_distance()}: Add cumulative \code{distance} (metres) from geodesic
point-to-point segments. When a device-reported \code{dev_dist} column is
present (TCX/FIT), also add \code{dist_diff} = \code{dev_dist} - \code{distance} for QA.

\item \code{addcols_speed()}: Add smoothed instantaneous \code{speed} (m/s), plus
\code{speed_kmh} and \code{pace} (min/km). Speed is a centred moving average of
point-to-point \code{diff(distance) / diff(time)} over \code{window} points either
side.

\item \code{addcols_speed_naive()}: Add naive average \code{speed_ms} (m/s) from cumulative
\code{distance / time}, plus \code{speed_kmh} and \code{pace} (min/km). Unsmoothed; this
is the running average from the start, not an instantaneous rate.

}}
\examples{
\dontrun{
read_stream("activities/9973795459.gpx.gz") |>
  addcols_time() |>
  addcols_distance() |>
  addcols_speed(window = 2) |>
  relocate_activity_cols()
}
}
