% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto_dynamic_dashboard.R
\name{qd_render_dashboard}
\alias{qd_render_dashboard}
\title{Render the Quarto dashboard}
\usage{
qd_render_dashboard(
  repo = here("strava_repo"),
  max_files = 10,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{Path to the Strava repository.}

\item{max_files}{Maximum number of per-activity data files to write in one
call.}

\item{force}{Re-render the Quarto pages even when they look current.}

\item{quiet}{Suppress progress reporting.}
}
\value{
Path to the dashboard index, invisibly.
}
\description{
Builds \verb{dashboard_qd/} inside the Strava repository: a static index shell, an
overview page carrying a client-side trends chart and activity table, and a
single detail page that renders whichever activity is named by its \verb{?id=}
query parameter.
}
\details{
The pages carry no data of their own. Everything they display is written to
\verb{dashboard_qd/data/} as JavaScript files, so adding activities rewrites data
and does not re-render any HTML. The result opens from disk without a web
server, and needs no network once built.

This runs alongside \code{\link[=rmd_render_dashboard]{rmd_render_dashboard()}}, which builds the older Rmd
dashboard into \verb{dashboard/}. The two share the manifest reader and the
Parquet statistics helper and are otherwise independent.
}
