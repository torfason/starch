% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto_static_dashboard.R
\name{qs_render_activities}
\alias{qs_render_activities}
\title{Render per-activity pages with Quarto}
\usage{
qs_render_activities(repo = here("strava_repo"), max_files = 10, quiet = FALSE)
}
\arguments{
\item{repo}{Path to the Strava repository.}

\item{max_files}{Maximum number of activities to render in one call.}

\item{quiet}{Suppress progress reporting. Note that this also suppresses
the Quarto CLI's own output, including the detail of a failed render.}
}
\value{
A tibble of the activities rendered, invisibly.
}
\description{
Renders one HTML page per activity into \verb{dashboard_qs/}, working backwards
from the most recent activity and stopping after \code{max_files}.
}
\details{
An activity is rendered when it has a Parquet stream file and does not yet
have an HTML page. As in the Rmd stack there is no content hashing: editing
the template will not rebuild an existing page. Delete the page to force one.
}
