% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_columns.R
\name{addcols_splits}
\alias{addcols_splits}
\title{Add rolling split columns}
\usage{
addcols_splits(
  d,
  distance = c(1, 5, 10),
  units = c("km", "m", "miles"),
  type = c("pace", "time")
)
}
\arguments{
\item{d}{A stream tibble carrying \code{distance} and \code{time}. If either is
absent, or there are too few distinct distances to interpolate between,
\code{d} is returned unchanged.}

\item{distance}{Window sizes, in \code{units}.}

\item{units}{Unit the windows are expressed in.}

\item{type}{Whether each column holds the pace across its window or the time
taken to cover it.}
}
\value{
\code{d} with one column appended per window that yielded any data.
}
\description{
Adds one column per window in \code{distance}, each holding a rolling measure
over the preceding window: either the pace across it, or the time taken to
cover it. Every value looks strictly backwards, so \code{min(pace_10k)} over an
activity is the fastest 10 km run within it.
}
\details{
The point exactly one window back almost never coincides with a recorded
sample, so the time at that distance is interpolated rather than snapped to
the nearest row. Values are \code{NA} over the opening stretch, where the window
would reach back beyond the start of the track.
}
\section{Column names and units}{

Names are the type, the window, and the unit suffix: \code{pace_1k}, \code{time_5k},
\code{pace_400m}, \code{pace_5mi}. Fractional windows replace the decimal point with
an underscore, so 21.1 km becomes \code{pace_21_1k}, which needs no backticks.

\code{type = "pace"} is reported per kilometre for \code{"km"} and \code{"m"}, and per mile
for \code{"miles"}, so a 400 m split reads as min/km rather than the useless
min/metre. This matches the units of the \code{pace} column from
\code{\link[=addcols_speed]{addcols_speed()}}, so the two can share an axis. \code{type = "time"} is in
seconds, matching the \code{time} column.

Windows longer than the activity produce nothing, and those columns are
dropped rather than carried as all-\code{NA}. Columns are appended in the order
given; use \code{\link[=relocate_activity_cols]{relocate_activity_cols()}} if a canonical order is wanted.
}

