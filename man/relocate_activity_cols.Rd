% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_columns.R
\name{relocate_activity_cols}
\alias{relocate_activity_cols}
\title{Reorder stream columns into the canonical activity layout}
\usage{
relocate_activity_cols(d)
}
\arguments{
\item{d}{A stream tibble, e.g. from \code{\link[=read_stream]{read_stream()}} after the \verb{addcols_*}
transforms.}
}
\value{
\code{d} with columns relocated; contents and row order unchanged.
}
\description{
Relocates whichever canonical stream columns are present into a fixed order
(axes, position, speed/pace, recorded sensors, device-reported channels, then
diagnostics). Uses \code{\link[dplyr:any_of]{dplyr::any_of()}}, so absent columns are skipped rather
than raising an error, and any column not in the canonical list is kept, in
its existing relative order, after the listed ones.
}
