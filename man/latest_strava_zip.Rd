% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strava_repo.R
\name{latest_strava_zip}
\alias{latest_strava_zip}
\title{Locate the most recent Strava export archive}
\usage{
latest_strava_zip(dir = here("strava_zips"), by = c("both", "name", "time"))
}
\arguments{
\item{dir}{Directory holding downloaded Strava export archives.}

\item{by}{How the most recent archive is identified. \code{"both"} (the default)
requires the name and modification time orderings to agree, \code{"name"} uses
the file name alone, and \code{"time"} the modification time alone.}
}
\value{
Path to the most recent \code{.zip} archive in \code{dir}.
}
\description{
Strava bulk exports arrive as a zip archive. This picks the most recent one
out of a directory of downloaded exports, and is the default source for
\code{\link[=strava_zip_to_repo]{strava_zip_to_repo()}}.
}
\details{
"Most recent" can mean either the last file name or the last modification
time. These usually agree, but need not: copying or restoring files rewrites
modification times, and name ordering only tracks chronology if the archives
are named so that they sort that way. The default (\code{"both"}) requires the two
to agree and errors when they do not, rather than silently picking one.
}
