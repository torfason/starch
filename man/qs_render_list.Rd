% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto_static_dashboard.R
\name{qs_render_list}
\alias{qs_render_list}
\title{Render the plain activity list}
\usage{
qs_render_list(repo = here("strava_repo"), verbose = FALSE, quiet = FALSE)
}
\arguments{
\item{repo}{Path to the Strava repository.}

\item{verbose}{Pass the Quarto CLI's own output through, which is verbose
but is the only way to see why a render failed.}

\item{quiet}{Suppress starch's own progress reporting. Independent of
\code{verbose}: the default reports progress without the CLI's output.}
}
\value{
Path to the written page, invisibly.
}
\description{
Writes \code{dashboard_qs/activity_list.html}, a table of every activity in the
manifest, linking to those that already have a page. The manifest is written
to the staged project as \code{manifest.csv} and read back by \code{list.qmd}, so the
template holds no knowledge of the repository layout.
}
\details{
This is the simplest of the overview pages, kept alongside the filterable
table so the two can be compared.
}
