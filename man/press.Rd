% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/press.R
\name{press}
\alias{press}
\title{Run the maintenance sequence}
\usage{
press(
  repo = here("strava_repo"),
  zip = latest_strava_zip(),
  max_parquet = 50,
  max_pages = 10,
  max_points = 600,
  update_heatmap = FALSE,
  confirm = TRUE,
  view = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{Path to the Strava repository.}

\item{zip}{Path to the export archive. Defaults to the most recent one, and
the import step is skipped if none is found.}

\item{max_parquet}{Default number of activities to convert to Parquet.}

\item{max_pages}{Default number of dashboard pages to render.}

\item{max_points}{Stream points kept per page, passed to the renderer.}

\item{update_heatmap}{Rebuild the heat map even when it already exists. It
reads every Parquet file, so it is skipped by default; see
\code{\link[=dash_update_heatmap]{dash_update_heatmap()}}.}

\item{confirm}{Ask before each step. \code{FALSE} runs the whole sequence on the
defaults without prompting.}

\item{view}{Offer to open the dashboard at the end.}

\item{quiet}{Suppress the underlying functions' progress reporting. The
prompts and step summaries are not affected.}
}
\value{
Path to the repository, invisibly, or \code{NULL} if aborted.
}
\description{
Imports the latest Strava export, converts new activities to Parquet,
renders the static dashboard, and offers to open it. Each step reports what
it is about to do and asks before doing it, so the whole run can be stepped
through, skipped past, or abandoned.
}
